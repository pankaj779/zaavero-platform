# GOAL

Complete the entire Teacher Portal.

This is NOT another module implementation sprint.

This is the final completion sprint for the Teacher Portal.

Everything that can be integrated with the existing NestJS backend should now be integrated.

Nothing should remain mocked unless the backend truly does not support it.

The Teacher Portal should be considered production-ready after this sprint.

---

# Existing Architecture

Keep the existing architecture exactly as it is.

Server Components

↓

Client Workspace

↓

Api Layer

↓

Mapper

↓

Frontend DTO

↓

React Components

↓

UI

Never bypass the API layer.

Never fetch inside React components.

Use apiFetch() only.

Never expose Prisma models.

---

# Complete every remaining Teacher module

Finish Analytics.

Replace every remaining mock.

Replace every remaining placeholder that the backend already supports.

Replace any duplicated temporary mapper values.

Complete every unfinished CRUD flow that already has backend endpoints.

Complete create/update/delete dialogs wherever backend APIs already exist.

Finish Details panels.

Finish empty/loading/error states.

Finish optimistic refresh after mutations.

Finish pagination support (UI if metadata already exists).

Remove all dead mock code.

---

# Bug sweep

Search the Teacher Portal for

TODO

FIXME

coming soon

placeholder

mock

temporary

stub

fake

sample

disabled

Implement everything that already has backend support.

Only leave placeholders where backend functionality genuinely does not exist.

---

# UX polish

Fix

loading flicker

refresh bugs

stale state

selection bugs

sorting inconsistencies

filter inconsistencies

optimistic updates

error handling

toast messages

retry actions

form validation

accessibility

responsive issues

keyboard navigation

focus management

---

# API cleanup

Remove duplicated API logic.

Remove duplicated mapper logic.

Reuse existing helper utilities.

Remove dead exports.

Remove dead DTOs.

Remove dead mock data.

---

# Performance

Memoize expensive selectors.

Avoid duplicate API requests.

Reuse cached lookups.

Remove unnecessary rerenders.

---

# Security

Verify every Teacher page is protected.

Verify ownership checks.

Verify organization isolation.

Verify permissions.

No page should expose another organization's data.

---

# Tests

Update all failing tests.

Add tests where missing.

Keep coverage consistent.

---

# Verification

Run

pnpm typecheck

pnpm lint

pnpm test

pnpm build

Everything must pass.

---

# Deliverable

Return

Architecture summary

Files created

Files modified

Files removed

Verification

Remaining technical debt

Recommended commit

Do not stop until the Teacher Portal is completely finished.
