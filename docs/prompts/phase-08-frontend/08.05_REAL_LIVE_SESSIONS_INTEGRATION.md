Task 08.05 — Real Teacher Live Sessions Integration

IMPORTANT

Replace ONLY the Teacher Live Sessions mock data with the real NestJS Live Session API.

Do NOT redesign the UI.

Do NOT change styling.

Do NOT modify the Teacher Shell.

Do NOT modify authentication.

Do NOT modify the Student Portal.

====================================================

Current state

Teacher Live Sessions workspace already exists.

Replace its mock data source with the NestJS API.

Keep every UI component exactly as implemented.

====================================================

Backend already exists

GET /api/v1/live-sessions

GET /api/v1/live-sessions/:id

POST /api/v1/live-sessions

PATCH /api/v1/live-sessions/:id

DELETE /api/v1/live-sessions/:id

Authentication is already integrated.

====================================================

Follow EXACTLY the architecture already used by

CourseApi

BatchApi

EnrollmentApi

LessonApi

No new architecture.

====================================================

Create

apps/web/lib/api/live-session.ts

apps/web/lib/api/live-session-mapper.ts

Export from

apps/web/lib/api/index.ts

====================================================

LiveSessionApi methods

getLiveSessions()

getLiveSession()

createLiveSession()

updateLiveSession()

deleteLiveSession()

Always use apiFetch().

Never call fetch() directly from React components.

====================================================

Create

apps/web/lib/teacher/live-session-types.ts

Move all frontend DTOs, helper functions, filters, stats, sorting, formatting, and API query mapping into this file.

Frontend DTOs must remain independent from backend DTOs.

====================================================

The workspace must support

Loading

Error

Empty

Real API data

Debounced search

Course filter

Batch filter

Status filter

Meeting provider filter (if supported)

Server-side filtering whenever possible.

====================================================

Keep every existing component unchanged

LiveClassesWorkspace

LiveClassCard

LiveClassCollection

LiveClassStats

LiveClassSearch

LiveClassFilters

LiveClassViewToggle

LiveClassStatusBadge

LiveClassDetailsPanel

LiveClassesHeader

LiveClassesSkeleton

LiveClassesEmptyState

LiveClassesErrorState

No redesign.

Only replace the data source.

====================================================

Delete the mock live-session source

ONLY after successful integration.

====================================================

Architecture

API

↓

Mapper

↓

Frontend DTO

↓

React Components

Never expose backend DTOs.

Never import Prisma types.

====================================================

Consume pagination metadata.

Do not build pagination UI.

====================================================

If backend fields are missing

meeting link

attendance rollups

recording URL

course title

batch title

mentor avatar

or other UI fields

keep temporary placeholders ONLY inside

live-session-mapper.ts

Never inside React components.

====================================================

Verification

Run

typecheck

lint

tests

build

Everything must pass.

Stop immediately after verification.

Return only

Architecture summary

Files created

Files modified

Mock files removed

Verification results

Technical debt

Recommended commit
