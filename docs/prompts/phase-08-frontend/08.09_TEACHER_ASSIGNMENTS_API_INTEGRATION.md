# Sprint 08.09 — Teacher Assignments API Integration

## Objective

Replace the Teacher Assignments workspace mock data with the real NestJS Assignment API while preserving the entire UI.

This is an API integration task only.

Do NOT redesign the page.

Do NOT modify the design system.

Do NOT modify routing.

Do NOT modify authentication.

Do NOT modify Student Portal.

Do NOT modify Admin Portal.

Only replace the data source.

---

## Existing Architecture

Follow exactly the same architecture used for:

- Teacher Courses
- Teacher Batches
- Teacher Students
- Teacher Lessons
- Teacher Live Sessions
- Teacher Attendance

The stack must remain:

AssignmentApi
↓

assignment-mapper

↓

TeacherAssignmentSummaryDto

↓

AssignmentsView

React components must never call fetch() directly.

Everything must go through apiFetch().

---

## Build

Create

apps/web/lib/api/assignment.ts

apps/web/lib/api/assignment-mapper.ts

apps/web/lib/api/assignment.test.ts

apps/web/lib/api/assignment-mapper.test.ts

apps/web/lib/teacher/assignment-types.ts

Update

apps/web/components/teacher/assignments/assignments-view.tsx

apps/web/lib/api/index.ts

apps/web/lib/teacher/index.ts

apps/web/lib/teacher/README.md

apps/web/app/page.test.ts

Remove

apps/web/lib/teacher/mock-assignments.ts

---

## API

Use the Nest Assignment API.

Support

GET assignments

GET assignment

createAssignment()

updateAssignment()

deleteAssignment()

Only list is consumed by the UI.

CRUD methods should still exist for future dialogs.

---

## Mapping

Convert backend DTOs into existing TeacherAssignmentSummaryDto.

Keep all UI components unchanged.

Fields missing from the backend should remain mapper placeholders only.

Never invent backend properties.

Examples of temporary placeholders:

submission counts

grading summary

attachment preview

teacher avatar

course thumbnail

timeline summary

Everything else must come from the API.

---

## Search / Filter / Sort

Continue using the backend whenever supported.

Use API filters for:

organization

course

batch

status

search

sort

Only perform client-side filtering if the backend cannot support a field.

---

## Stats

Load statistics from the API.

Reuse the existing pattern used by Teacher Courses.

No mock statistics.

---

## Performance

Debounce search.

Memoize mapper output.

Consume pagination metadata.

Do not build pagination UI yet.

No unnecessary requests.

---

## Accessibility

No regressions.

Keep

ARIA labels

focus handling

keyboard navigation

Escape behavior

reduced motion

semantic markup

---

## Do NOT

Redesign UI

Rename components

Change styling

Modify shared primitives

Touch Student Portal

Touch Admin Portal

Touch authentication

Touch backend

Touch Prisma

Touch APIs

---

## Verification

Run

pnpm --filter @graphology/web typecheck

pnpm --filter @graphology/web lint

pnpm --filter @graphology/web test

pnpm --filter @graphology/web build

All must pass.

---

## Deliver

Return only

Architecture summary

Files created

Files modified

Mock files removed

Verification results

Technical debt

Recommended commit message

No code dump.
