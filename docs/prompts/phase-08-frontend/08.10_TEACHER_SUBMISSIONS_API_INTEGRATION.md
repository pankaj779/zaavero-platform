# Sprint 08.10 — Teacher Submissions API Integration

## Objective

Replace the Teacher Submissions workspace mock data with the real NestJS Submission API while preserving the existing UI.

This is an API integration sprint only.

Do NOT redesign the UI.

Do NOT modify the design system.

Do NOT modify authentication.

Do NOT modify Student Portal.

Do NOT modify Admin Portal.

Only replace the data source.

---

## Existing Architecture

Follow exactly the same architecture already implemented for

- Courses
- Batches
- Students
- Lessons
- Live Sessions
- Attendance
- Assignments

The stack must remain

SubmissionApi

↓

submission-mapper

↓

TeacherSubmissionSummaryDto

↓

SubmissionsView

No React component may call fetch() directly.

Everything must use apiFetch().

---

## Build

Create

apps/web/lib/api/submission.ts

apps/web/lib/api/submission-mapper.ts

apps/web/lib/api/submission.test.ts

apps/web/lib/api/submission-mapper.test.ts

apps/web/lib/teacher/submission-types.ts

Update

apps/web/components/teacher/submissions/submissions-view.tsx

apps/web/lib/api/index.ts

apps/web/lib/teacher/index.ts

apps/web/lib/teacher/README.md

apps/web/app/page.test.ts

Remove

apps/web/lib/teacher/mock-submissions.ts

---

## API

Use the existing NestJS Submission API.

Support

GET submissions

GET submission

createSubmission()

updateSubmission()

Only GET list is consumed by the UI today.

CRUD methods should still exist for future grading dialogs.

---

## Mapping

Map backend DTOs into the existing TeacherSubmissionSummaryDto.

Keep every UI component unchanged.

Backend is the source of truth.

Only placeholder fields may remain for data not yet returned by the API.

Examples

student avatar

teacher avatar

file preview thumbnails

inline grading history

rubric summary

Never invent backend fields.

---

## Search / Filter / Sort

Prefer server-side filtering whenever supported.

Use API filters for

organization

assignment

student

status

search

sort

Only finish filtering client-side if the backend lacks support.

---

## Statistics

Load statistics from the API using the same pattern as Courses and Assignments.

No mock statistics.

Where backend aggregate endpoints do not yet exist, derive only from the fetched API list.

---

## Performance

Debounced search

Memoized mapping

Consume pagination metadata

No pagination UI yet

Avoid duplicate requests

---

## Accessibility

No regressions.

Maintain

ARIA labels

keyboard navigation

Escape behavior

focus management

semantic markup

reduced-motion support

---

## Do NOT

Redesign UI

Rename components

Modify styling

Touch shared primitives

Touch Student Portal

Touch Admin Portal

Touch authentication

Modify backend

Modify Prisma

Modify APIs

---

## Verification

Run

pnpm --filter @graphology/web typecheck

pnpm --filter @graphology/web lint

pnpm --filter @graphology/web test

pnpm --filter @graphology/web build

All must pass.

---

## Deliver

Return only

Architecture summary

Files created

Files modified

Mock files removed

Verification results

Technical debt

Recommended commit message

No code dump.
