Task 08.06 — Real Teacher Attendance Integration

IMPORTANT

Replace ONLY the Teacher Attendance mock data with the real NestJS Attendance API.

Do NOT redesign the UI.

Do NOT modify styling.

Do NOT touch Teacher Shell.

Do NOT touch authentication.

Do NOT touch Student Portal.

====================================================

Current state

Teacher Attendance workspace already exists.

Replace its mock data with the NestJS Attendance API.

Keep every component exactly as implemented.

====================================================

Backend already exists

GET /api/v1/attendances

GET /api/v1/attendances/:id

POST /api/v1/attendances

PATCH /api/v1/attendances/:id

Authentication already works.

====================================================

Follow EXACTLY the architecture already used by

CourseApi

BatchApi

EnrollmentApi

LessonApi

LiveSessionApi

Do not invent a new architecture.

====================================================

Create

apps/web/lib/api/attendance.ts

apps/web/lib/api/attendance-mapper.ts

Export from

apps/web/lib/api/index.ts

====================================================

AttendanceApi methods

getAttendances()

getAttendance()

createAttendance()

updateAttendance()

Always use apiFetch().

Never call fetch() inside React components.

====================================================

Create

apps/web/lib/teacher/attendance-types.ts

Move frontend DTOs, filters, stats, formatting, sorting, query mapping, helper utilities into this file.

Frontend DTOs must remain independent from backend DTOs.

====================================================

Workspace requirements

Loading

Error

Empty

Real API data

Debounced search

Status filter

Course filter

Batch filter

Live session filter (when available)

Server-side filtering whenever supported.

Client-side only when backend does not expose the filter.

====================================================

Keep every existing component unchanged

AttendanceWorkspace

AttendanceSessionCard

AttendanceCollection

AttendanceStats

AttendanceSearch

AttendanceFilters

AttendanceStatusBadge

AttendanceDetailsPanel

AttendanceHeader

AttendanceSkeleton

AttendanceEmptyState

AttendanceErrorState

Do not redesign anything.

Only replace the data source.

====================================================

Delete

apps/web/lib/teacher/mock-attendance.ts

ONLY after successful integration.

====================================================

Architecture

AttendanceApi

↓

attendance-mapper

↓

Frontend DTO

↓

Existing React components

Never expose backend DTOs.

Never expose Prisma models.

====================================================

Consume pagination metadata.

Do not implement pagination UI.

====================================================

If backend fields are missing

student name

student avatar

course title

batch title

attendance percentage

teacher name

session metadata

or other UI-only fields

keep temporary placeholders ONLY inside

attendance-mapper.ts

Never inside React components.

====================================================

Verification

Run

typecheck

lint

tests

build

Everything must pass.

Stop immediately after verification.

Return only

Architecture summary

Files created

Files modified

Mock files removed

Verification results

Technical debt

Recommended commit
