Task 08.03 — Real Teacher Student Integration

IMPORTANT

Replace ONLY the Teacher Students mock data with the real NestJS Enrollment API.

Do NOT redesign the UI.
Do NOT change styling.
Do NOT modify the Teacher Shell.
Do NOT modify the Student Portal.
Do NOT modify authentication.

====================================================

Current state

Teacher Students still uses

lib/teacher/mock-students.ts

Replace it completely.

====================================================

Backend already exists

GET /api/v1/enrollments

GET /api/v1/enrollments/:id

POST /api/v1/enrollments

PATCH /api/v1/enrollments/:id

DELETE /api/v1/enrollments/:id

Authentication already exists.

====================================================

Reuse the same architecture used by

CourseApi

BatchApi

Do not duplicate logic.

====================================================

Create

lib/api/enrollment.ts

lib/api/enrollment-mapper.ts

Export them from

lib/api/index.ts

====================================================

EnrollmentApi

Methods

getEnrollments()

getEnrollment()

createEnrollment()

updateEnrollment()

deleteEnrollment()

Always use apiFetch().

Never use fetch() inside React components.

====================================================

Create

lib/teacher/student-types.ts

Move all frontend DTOs, query mapping, stats helpers, filters, sort mapping, and utility functions here.

Frontend DTOs must remain independent from backend DTOs.

====================================================

The Teacher Students workspace must support

Loading

Error

Empty

Real API data

Debounced search

Status filter

Sorting

Server-side filtering whenever supported

====================================================

Keep every existing component

StudentCard

StudentCollection

StudentStats

StudentAvatar

StatusBadge

Search

Filters

View Toggle

Skeleton

Empty State

Error State

Exactly as they are.

Only replace the data source.

====================================================

Delete

lib/teacher/mock-students.ts

ONLY after everything works.

====================================================

Never expose backend DTOs to React components.

Backend DTO

↓

Mapper

↓

Frontend DTO

↓

Components

====================================================

No Prisma types.

No backend DTO imports.

====================================================

Consume pagination metadata returned by the API.

No pagination UI yet.

====================================================

Where the Enrollment API does not yet provide data required by the UI
(attendance %, assignment count, progress %, avatar, at-risk flag, etc.)

keep clearly marked temporary placeholders inside the mapper only.

Do NOT place placeholder logic inside React components.

====================================================

Verification

typecheck

lint

tests

build

must pass.

Stop after verification.

Return only

Architecture summary

Files created

Files modified

Mock files removed

Verification results

Technical debt

Recommended commit
