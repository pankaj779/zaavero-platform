Task 08.04 — Real Teacher Lesson Integration

IMPORTANT

Replace ONLY the Teacher Lessons mock data with the real NestJS Lesson API.

Do NOT redesign the UI.

Do NOT change styling.

Do NOT modify the Teacher Shell.

Do NOT modify the Student Portal.

Do NOT modify authentication.

====================================================

Current state

Teacher Lessons still renders from

lib/teacher/mock-lessons.ts

(or the current lesson mock source used by the Teacher portal)

Replace it completely.

====================================================

Backend already exists

GET /api/v1/lessons

GET /api/v1/lessons/:id

POST /api/v1/lessons

PATCH /api/v1/lessons/:id

DELETE /api/v1/lessons/:id

Authentication already exists.

====================================================

Follow EXACTLY the same architecture as

CourseApi

BatchApi

EnrollmentApi

Do not introduce a new pattern.

====================================================

Create

apps/web/lib/api/lesson.ts

apps/web/lib/api/lesson-mapper.ts

Export them from

apps/web/lib/api/index.ts

====================================================

LessonApi

Methods

getLessons()

getLesson()

createLesson()

updateLesson()

deleteLesson()

Always use apiFetch().

Never call fetch() from React components.

====================================================

Create

apps/web/lib/teacher/lesson-types.ts

Move all frontend DTOs, filters, sorting helpers, query mapping, statistics helpers, and formatting utilities here.

Frontend DTOs must remain independent from backend DTOs.

====================================================

Teacher Lessons workspace must support

Loading

Error

Empty

Real API data

Debounced search

Course filter (if supported)

Module filter (if supported)

Content-type filter

Sorting

Server-side filtering whenever supported.

====================================================

Keep every existing component exactly as-is

LessonCard

LessonCollection

LessonStats

LessonSearch

LessonFilters

LessonViewToggle

LessonHeader

LessonSkeleton

LessonEmptyState

LessonErrorState

LessonDetailsPanel

No redesign.

Only replace the data source.

====================================================

Delete

the lesson mock file

ONLY after the integration is fully working.

====================================================

Never expose backend DTOs to React components.

Backend DTO

↓

Mapper

↓

Frontend DTO

↓

React Components

====================================================

No Prisma types.

No backend DTO imports.

====================================================

Consume pagination metadata from the API.

Do not build pagination UI yet.

====================================================

If the backend does not yet provide fields required by the UI
(duration, thumbnail, attachment count, completion count, etc.)

keep temporary placeholders ONLY inside lesson-mapper.ts.

Never place placeholder logic inside React components.

====================================================

Verification

typecheck

lint

tests

build

must all pass.

Stop after verification.

Return only

Architecture summary

Files created

Files modified

Mock files removed

Verification results

Technical debt

Recommended commit
