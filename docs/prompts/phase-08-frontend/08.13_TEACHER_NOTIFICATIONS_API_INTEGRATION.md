# Sprint 08.13 — Teacher Notifications API Integration

## Objective

Replace the Teacher Notifications workspace mock data with the real NestJS Notification API while preserving the existing UI.

This is an API integration sprint only.

Do NOT redesign the UI.

Do NOT modify shared components.

Do NOT modify authentication.

Do NOT modify Student Portal.

Do NOT modify Admin Portal.

Only replace the data source.

---

## Existing Architecture

Follow the exact architecture already used for

- Courses
- Batches
- Students
- Lessons
- Live Sessions
- Attendance
- Assignments
- Submissions
- Certificates
- Calendar

The stack must remain

NotificationApi

↓

notification-mapper

↓

TeacherNotificationDto

↓

NotificationsView

React components must never call fetch() directly.

Everything must use apiFetch().

---

## Build

Create

apps/web/lib/api/notification.ts

apps/web/lib/api/notification-mapper.ts

apps/web/lib/api/notification.test.ts

apps/web/lib/api/notification-mapper.test.ts

apps/web/lib/teacher/notification-types.ts

Update

apps/web/components/teacher/notifications/notifications-view.tsx

apps/web/lib/api/index.ts

apps/web/lib/teacher/index.ts

apps/web/lib/teacher/README.md

apps/web/app/page.test.ts

Remove

apps/web/lib/teacher/mock-notifications.ts

---

## API

Use the existing NestJS Notification API.

Support

GET notifications

GET notification

PATCH notification

PATCH mark-read

POST read-all

Only GET list is consumed today.

Keep all API methods for future UI actions.

---

## Mapping

Convert backend DTOs into the existing TeacherNotificationDto.

Backend is the source of truth.

Keep every UI component unchanged.

Only mapper placeholders may remain for information not yet returned.

Examples

sender avatar

course thumbnail

deep links

email delivery state

push delivery state

Never invent backend fields.

---

## Search / Filter / Sort

Prefer backend filtering whenever supported.

Use API filters for

organization

read status

notification type

search

sort

Only finish filtering client-side when required.

---

## Statistics

Use the backend list.

If aggregate endpoints do not exist,

derive

Unread count

Read count

Today's notifications

This week's notifications

from the fetched API list.

No mock statistics.

---

## Performance

Debounced search

Memoized mapping

Consume pagination metadata

No pagination UI

Avoid duplicate API requests

---

## Accessibility

No regressions.

Maintain

- ARIA labels
- keyboard navigation
- Escape behavior
- focus management
- semantic markup
- reduced-motion support

---

## Do NOT

Redesign UI

Modify backend

Modify Prisma

Modify APIs

Modify Student Portal

Modify Admin Portal

Modify authentication

---

## Verification

Run

pnpm --filter @graphology/web typecheck

pnpm --filter @graphology/web lint

pnpm --filter @graphology/web test

pnpm --filter @graphology/web build

All must pass.

---

## Deliver

Return only

Architecture summary

Files created

Files modified

Mock files removed

Verification results

Technical debt

Recommended commit message

No code dump.
