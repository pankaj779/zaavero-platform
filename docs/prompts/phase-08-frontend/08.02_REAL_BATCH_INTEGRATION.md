Task 08.02 — Real Teacher Batch Integration

IMPORTANT

Replace ONLY the Teacher Batches mock data with the real NestJS Batch API.

Do NOT redesign the UI.
Do NOT change styling.
Do NOT modify the Teacher Shell.
Do NOT modify the Student Portal.
Do NOT touch authentication.

====================================================

Current state

Teacher Batches still uses

lib/teacher/mock-batches.ts

Replace it completely.

====================================================

Backend already exists

GET /api/v1/batches

GET /api/v1/batches/:id

POST /api/v1/batches

PATCH /api/v1/batches/:id

DELETE /api/v1/batches/:id

Authentication is already integrated.

====================================================

Reuse the existing architecture introduced for Courses.

Do NOT duplicate logic.

Follow the same folder structure.

====================================================

Create

lib/api/batch.ts

lib/api/batch-mapper.ts

Export from

lib/api/index.ts

====================================================

Create

BatchApi

Methods

getBatches()

getBatch()

createBatch()

updateBatch()

deleteBatch()

Use apiFetch().

Never call fetch directly inside React components.

====================================================

Create

lib/teacher/batch-types.ts

Move all frontend-only DTOs, filters, sort mapping, stats helpers, and query mapping here.

Keep frontend DTOs independent from backend DTOs.

====================================================

CoursesView became the reference.

Mirror that architecture.

====================================================

Batch workspace must support

Loading

Error

Empty

Real API data

Debounced search

Status filter

Sort

Server-side filtering whenever supported

====================================================

Keep

BatchCard

BatchCollection

BatchStats

Search

Filters

View Toggle

Badges

Skeleton

Empty State

Error State

Exactly as they are.

Only replace the data source.

====================================================

Delete

lib/teacher/mock-batches.ts

ONLY after the module is fully working.

====================================================

Do not expose API payloads to components.

Always map API DTO

↓

Frontend DTO

====================================================

No Prisma types.

No backend DTO imports.

====================================================

Keep pagination ready.

Consume pagination metadata returned by the API even if the UI does not expose paging yet.

====================================================

Verification

typecheck

lint

tests

build

must pass.

Stop after verification.

Return only

Architecture summary

Files created

Files modified

Mock files removed

Verification results

Technical debt

Recommended commit
