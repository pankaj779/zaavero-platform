Task 08.01 — Real Teacher Course Integration

IMPORTANT

This is the first REAL frontend/backend integration.

Do NOT redesign UI.
Do NOT change styling.
Do NOT change UX.
Do NOT change routing.
Do NOT modify Student Portal.

Replace ONLY the mock data inside Teacher Courses with the real NestJS API.

====================================================

Current State

Teacher Courses currently renders:

lib/teacher/mock-courses.ts

Replace it completely.

====================================================

Backend

Already available

GET /api/v1/courses
GET /api/v1/courses/:id
POST /api/v1/courses
PATCH /api/v1/courses/:id
DELETE /api/v1/courses/:id

Authentication already exists.

====================================================

Requirements

Create a dedicated API layer.

Example

lib/api/course.ts

Never call fetch directly inside React components.

====================================================

Create

CourseApi

Methods

getCourses()

getCourse()

createCourse()

updateCourse()

deleteCourse()

====================================================

Use existing

apiFetch()

from auth layer.

====================================================

Workspace

Teacher Courses should

load

loading

error

empty

real data

without changing the UI.

====================================================

Delete

mock-courses.ts

ONLY after every screen works correctly.

====================================================

Search

Status filter

Sorting

Pagination

must now use server responses instead of local mock helpers whenever supported by the API.

====================================================

Keep DTO mapping isolated.

Never expose API responses directly to components.

====================================================

No Prisma types.

No backend DTO imports.

Frontend owns its own mapping.

====================================================

Keep all components.

CourseCard

CourseCollection

CourseStats

Search

Filters

View Toggle

Everything remains.

Only data source changes.

====================================================

Verification

Typecheck

Lint

Tests

Build

must pass.

Stop after verification.

Return

Architecture summary

Files created

Files modified

Mock files removed

Verification results

Technical debt

Recommended commit
