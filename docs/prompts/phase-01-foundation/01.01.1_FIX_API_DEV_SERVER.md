# ENGINEERING TASK 01.01.1

# Fix API Development Server

## Background

The project bootstrap completed successfully.

The frontend starts correctly.

The backend does not start in development mode.

Current error:

Error: Cannot find module 'apps/api/dist/main'

Compilation succeeds but the runtime attempts to execute the compiled dist output instead of running the NestJS development server.

---

## Goal

Fix the development workflow so the NestJS API starts correctly.

---

## Requirements

Investigate the following:

- apps/api/package.json
- root package.json
- turbo.json
- tsconfig files
- NestJS configuration
- development scripts
- build scripts
- watch scripts

Determine why dev mode is attempting to execute

dist/main

instead of using the NestJS development server.

---

## Expected Behaviour

Running

pnpm dev

should start

Frontend

↓

Backend

↓

Both remain in watch mode.

The API should expose

GET /

GET /health

GET /status

Return simple JSON responses.

Example

GET /

{
  "service":"Graphology Platform API",
  "status":"running",
  "version":"0.1.0"
}

GET /health

{
  "status":"healthy",
  "timestamp":"..."
}

---

## Constraints

Do not implement authentication.

Do not implement Prisma models.

Do not implement business logic.

Do not implement database connections.

Do not implement any frontend UI.

Only repair the development environment.

---

## Validation

After fixing

Run

pnpm dev

Verify

http://localhost:3000

opens.

Verify

http://localhost:3001

returns JSON.

Verify

pnpm build

passes.

Verify

pnpm lint

passes.

Verify

pnpm typecheck

passes.

---

## Deliverables

Provide

Root cause

Files modified

Reason for modification

Commands executed

Test results

Recommended commit message

Stop after successful verification.