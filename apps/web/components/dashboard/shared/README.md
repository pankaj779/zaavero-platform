# Shared dashboard primitives

Reusable UI shells for student portal modules.

- `DashboardSearch` — search input wrapper
- `DashboardStatusSortFilters` — status + sort selects
- `DashboardStatGrid` — statistics cards row
- `DashboardEmptyState` — empty Card + EmptyState shell

Prefer these over per-module copies when building new domains.
