# Graphology mobile environment.
# Expo only exposes variables prefixed with EXPO_PUBLIC_ to the client bundle.

# Base URL of the existing NestJS API (reused by web + mobile).
# For a device/emulator, use your machine's LAN IP, not localhost.
EXPO_PUBLIC_API_URL=http://localhost:3001/api/v1
